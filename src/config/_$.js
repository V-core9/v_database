

const d_dir = '.v_database';
const d_cfg = 'v_config';
const cfg_dpath = process.env.home+'/'+d_dir+'/'+d_cfg;
const cfg_fpath = process.env.home+'/'+d_dir+'/'+d_cfg;

module.exports = {
  d_dir,
  d_cfg,
  cfg_dpath,
  cfg_fpath
};
